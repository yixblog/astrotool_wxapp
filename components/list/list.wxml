<view class="am-list {{className}}">
  <view class="am-list-header" wx:if="{{$slots.header}}">
    <slot name="header" />
  </view>
  <view class="am-list-body">
    <slot />
  </view>
  <view class="am-list-footer" wx:if="{{$slots.footer}}">
    <slot name="footer" />
  </view>
</view>
